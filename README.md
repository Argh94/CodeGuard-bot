# CodeGuard-bot

---
# CodeGuard – ربات تلگرام مدیریت گروه‌های کد با قوانین سخت‌گیرانه

> **ربات قدرتمند و حریم‌خصوصی‌محور** ساخته‌شده با **Cloudflare Workers**  
> برای اجرای **قوانین تمیز و فقط کد** در گروه‌های تلگرام.
یک ربات تلگرام ساده با Cloudflare Worker که پیام‌های کاربران را دریافت می‌کند و واکنش 👍 می‌فرستد، و پیام تبلیغاتی با لینک گروه و اطلاعات تماس تنها در **چت خصوصی (پی وی)** کاربر ارسال می‌کند.
---

## قابلیت‌های ربات

| # | قابلیت | توضیح |
|---|--------|-------|
| 1 | **ری‌اکشن 👍 خودکار به همه پیام‌ها** | به هر پیام یک 👍 اضافه می‌شود.<br>• در **گروه**: همه پیام‌ها<br>• در **چت خصوصی**: فقط پیام‌های غیربات |
| 2 | **پاسخ خودکار در چت خصوصی** | ارسال پیام خوش‌آمد با:<br>• سلام<br>• راهنمایی تماس با `@m4tinbeigipv` برای تبلیغ<br>• دکمه **"عضویت در گروه"** |
| 3 | **پاکسازی پیام‌های سیستمی** | **حذف خودکار** پیام‌های سیستم:<br>• عضویت جدید<br>• خروج عضو<br>• تغییر عنوان/عکس گروه<br>• ایجاد گروه |
| 4 | **پیام خوش‌آمدگویی خصوصی** | ارسال **قوانین + خوش‌آمد** به عضو جدید در **چت خصوصی** |
| 5 | **قوانین سخت‌گیرانه رسانه (غیرادمین)** | **حذف** هر یک از موارد زیر از غیرادمین‌ها:<br>• استیکر<br>• گیف/انیمیشن<br>• ویس<br>• فایل<br>• پیام فوروارد شده<br>• پیام متنی بدون عکس/ویدیو |
| 6 | **محدودیت کپشن به ۵ کلمه** | برای **عکس/ویدیو**:<br>• کپشن باید **حداکثر ۵ کلمه** داشته باشد<br>• کپشن طولانی → **حذف + گزارش** |
| 7 | **ممنوعیت لینک (غیرادمین)** | مسدود کردن **هر نوع لینک** در متن یا entity:<br>• `http://`, `https://`<br>• `t.me/`<br>• `www.`<br>• لینک‌های URL<br>• منشن با `t.me` |
| 8 | **فیلتر کلمات ممنوعه و خاص** | مسدود کردن پیام‌هایی که شامل **کلمات ممنوعه** باشند (حساس به حروف کوچک/بزرگ):<br>`فحش`, `توهین`, `ادمین`, `مدیر`, `admin`, `جنده`, `کص`, `سگ`, `کون` و **بیش از ۳۰ کلمه دیگر** |
| 9 | **سیستم گزارش تخلفات** | برای هر پیام حذف‌شده:<br>1. **فوروارد پیام اصلی** به `REPORT_USER_ID`<br>2. ارسال **اخطار موقت** در گروه: `نباید کپشن بیش از 5 کلمه بنویسی. پیامت حذف شد @username.`<br>3. **حذف خودکار اخطار پس از ۱۰ ثانیه** |
| 10 | **دستورات ادمینی** | فقط ادمین‌ها می‌توانند استفاده کنند:<br>• `/stats` → نمایش **تعداد اعضای گروه**<br>• `/pin_rules` → ارسال و **پین کردن قوانین گروه** |
| 11 | **تشخیص دقیق ادمین** | با استفاده از `getChatAdministrators` → **قوانین فقط برای غیرادمین‌ها اعمال می‌شود** |
| 12 | **حذف خودکار پیام‌های لاگ** | تمام اخطارهای گروه **پس از ۱۰ ثانیه حذف می‌شوند** |

---

## جریان مدیریت تخلف

```text
1. تشخیص تخلف
2. فوروارد پیام اصلی به ادمین گزارش (REPORT_USER_ID)
3. ارسال اخطار در گروه: "نباید کپشن بیش از 5 کلمه بنویسی. پیامت حذف شد @username."
4. حذف پیام اصلی
5. حذف اخطار پس از ۱۰ ثانیه```

## ویژگی‌ها

- ارسال خودکار واکنش 👍 به هر پیام دریافتی.
- ارسال پیام تبلیغاتی با دکمه شیشه‌ای (Inline Keyboard) فقط در دایرکت کاربران.
- جلوگیری از ارسال پیام تبلیغاتی برای ربات‌ها یا خود ربات.
- طراحی شده برای اجرا روی Cloudflare Workers با امنیت بالا (استفاده از Environment Variables).

---

## نصب و راه‌اندازی

1. **ایجاد ربات تلگرام** و دریافت `BOT_TOKEN` و `BOT_USERNAME`.

2. **تنظیم Environment Variables در Cloudflare Worker:**

```
BOT_TOKEN=توکن-ربات
BOT_USERNAME=نام-کاربری-ربات
```

3. **آپلود کد به Cloudflare Worker** و تنظیم مسیر `/webhook` برای دریافت پیام‌ها.

4. **ست کردن وب‌هوک تلگرام:**

```
https://api.telegram.org/bot<BOT_TOKEN>/setWebhook?url=https://your-domain.com/webhook
```

> جایگزین کنید:
> - `<BOT_TOKEN>` با توکن ربات خودتان
> - `https://your-domain.com/webhook` با مسیر Cloudflare Worker شما

5. **تست ربات**: هر پیام جدید به ربات واکنش 👍 می‌گیرد و اگر پیام از کاربر خصوصی باشد، پیام تبلیغاتی با لینک گروه دریافت می‌کند.

---

## پیام تبلیغاتی نمونه

```
سلام! 👋
برای تبلیغ لطفا به @Argh94 پیام بدید و برای عضویت در گروه زیر کلیک کنید:
[عضویت در گروه](https://t.me/+8UV5f9QfeLSgkxmNjg0)
```

---

## نکات مهم

- دکمه شیشه‌ای لینک گروه فقط در پی وی کاربر نمایش داده می‌شود.
- ربات پیام تبلیغاتی را به ربات‌ها یا خودش ارسال نمی‌کند.
- می‌توانید متن پیام تبلیغاتی را بر اساس نیاز خود تغییر دهید.
